category: screw

model: yolov11n.pt

confidence_threshold: 0.35
iou_threshold: 0.45

expected_classes:
  - screw

defect_classes:
  - manipulated_front
  - scratch_head
  - scratch_neck
  - thread_side
  - thread_top

min_defect_size: 20

input_size: 640

save_crops: true
save_labels: true
save_predictions: true